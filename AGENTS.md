# Vibe-Tech-Decision Agent Guidance

本仓库用于**增强技术判断质量**，不是替代模型判断，也不是技术政策引擎。

## 1. 权威顺序

在具体项目里做技术判断时，优先级是：

1. 当前项目的代码、运行数据、测试、约束和正式治理文档
2. 对应技术的官方一手资料、标准或原始规范
3. 与当前场景高度相似的成熟真实实现和生产证据
4. 本仓库的 source catalog 与 decision lenses
5. 模型自身已有知识与一般经验

如果这些信息冲突，不要机械按优先级选一个答案；应说明冲突并判断哪个信息对当前问题最相关、最新、最直接。

## 2. 本仓库不能规定最终答案

`decisions/*.md` 是 **decision lenses**：

- 用来提醒需要检查哪些维度
- 用来发现常见候选和 trade-off
- 用来提示可能遗漏的成本、风险或验证项

它们**不是默认技术栈**，也不构成以下结论：

- “必须先用现有数据库”
- “单向通信必须用 SSE”
- “不能用 Redis”
- “不能用 Kubernetes”
- “不能用 Agent / Multi-Agent”
- “已有前端不能重构”
- “必须先买后做”

如果当前项目证据支持不同答案，可以偏离任何启发式，只需把理由和证据说清楚。

## 3. Source catalog 不是白名单

`sources/source-catalog.yaml` 是经过筛选的研究起点。

AI 可以并且应该主动发现：

- 更直接的官方文档
- 某个候选技术自己的官方仓库
- 与目标项目更相似的高质量开源实现
- 最新标准、RFC、benchmark、事故复盘或生产案例

不要因为新来源 Star 较低就自动排除；先判断它是否是**一手来源、官方来源或当前问题的直接证据**。

同样，不要因为某来源 Star 很高就把它当成结论。

## 4. Star 的作用

本仓库使用 30k / 10k 作为**市场验证启发式**，来源于本项目对“避免未经市场验证的个人仓库作为重大参考”的偏好。

它不是行业标准，也不是可信度分数。

Star 更适合回答：

> 这个社区项目是否经过较大规模关注和筛选？

它不能直接回答：

> 这个项目对当前架构判断是否正确？

官方规范、一手文档、标准组织和技术自身官方仓库不受 Star 阈值限制。

## 5. 建议的判断过程

根据问题复杂度自由裁剪，但重大技术判断通常应覆盖：

- 当前真正的问题是什么
- 哪些是事实，哪些是假设
- 成功标准是什么
- 有哪些合理候选，包括“保持现状”
- 各候选解决什么，不解决什么
- 关键 trade-off
- 引入和长期维护成本
- 迁移、兼容和退出成本
- 安全、可靠性、性能或运维影响（若相关）
- 主要证据来源
- 如何验证最终选择

这是一组检查项，不要求固定顺序，也不要求每次全部输出。

## 6. 重大修改的研究深度

对于更换框架、数据库、核心通信模型、认证体系、存储体系，或引入重要基础设施/AI 架构等重大修改，应提高证据标准。

通常应尽量获得：

- 目标项目本地事实或测量
- 对应技术官方一手资料
- 一个或多个成熟实现/生产案例
- 对替代方案的公平比较

“必须有 30k+ Star 项目”不是硬条件；如果更直接的一手证据更强，应优先一手证据。

## 7. 避免弱推理

以下内容可以作为线索，但不能单独构成结论：

- “更现代”
- “更企业级”
- “以后可能需要”
- “大厂都在用”
- “某个高星仓库这么做”
- “benchmark 更快”但测试条件不同
- “AI 以前见过类似项目这么做”

需要继续结合当前项目事实与场景。

## 8. 输出要求

当用户需要技术决策时，优先给出真正影响选择的信息，而不是为了遵循模板而堆满栏目。

复杂决策应能区分：

- 事实
- 推断
- 未知项
- 建议
- 证据
- 验证方式

如果证据不足，可以提出暂定判断，也可以明确多个方案都合理；不要为了显得确定而伪造唯一答案。
